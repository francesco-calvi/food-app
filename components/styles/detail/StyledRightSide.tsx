import styled from "styled-components";
import { FlexCntAlgnCntr } from "../FlexStyles";

const StyledRightSide = styled(FlexCntAlgnCntr)`
  flex-direction: column;
  margin-left: 80px;
`;

export default StyledRightSide;
